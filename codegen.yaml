schema: ./src/graphql/schema/schema.graphql
generates:
  .//src/graphql/schema/generate/resolvers-types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers